const NAV=[["dashboard","▦","Tổng quan"],["tournaments","🏆","Giải đấu"],["players","👥","VĐV & cặp đấu"],["matches","🎾","Lịch & trận đấu"],["scores","✎","Nhập điểm"],["standings","≡","Bảng xếp hạng"],["bracket","⑂","Bracket"],["courts","▤","Sân thi đấu"],["referees","⚑","Trọng tài"],["payments","₫","Thanh toán"],["audit","☷","Nhật ký"],["settings","⚙","Cấu hình"]];

const seed={tournaments:[{id:"t1",name:"Saigon Pickle Open 2026",date:"27/09/2026",venue:"Pickle Hub Bình Thạnh",format:"Doubles 3.0–3.5",status:"live",teams:16},{id:"t2",name:"CLB Bình Lợi Championship",date:"04/10/2026",venue:"CLB Bình Lợi",format:"Mixed Doubles",status:"open",teams:24},{id:"t3",name:"Sunday Social Cup #12",date:"13/09/2026",venue:"Court 88",format:"Doubles Open",status:"done",teams:12}],teams:[{id:"A1",name:"Minh Búa / Quốc Anh",club:"Bình Lợi",group:"A",w:1,l:0,pf:22,pa:16},{id:"A2",name:"Hoàng Nam / Tuấn Kiệt",club:"Gò Vấp",group:"A",w:1,l:1,pf:48,pa:47},{id:"A3",name:"Gia Huy / Đức Long",club:"Phú Nhuận",group:"A",w:0,l:1,pf:25,pa:33},{id:"B1",name:"Bảo Minh / Trọng Nhân",club:"Thủ Đức",group:"B",w:0,l:0,pf:0,pa:0},{id:"B2",name:"Thanh Tùng / Hải Đăng",club:"Bình Lợi",group:"B",w:0,l:0,pf:0,pa:0},{id:"B3",name:"Quang Huy / Anh Khoa",club:"Tân Bình",group:"B",w:0,l:0,pf:0,pa:0}],matches:[{id:"m1",stage:"Bảng A",court:1,time:"18:00",a:"A1",b:"A2",status:"done",sets:[[11,7],[11,9]],winner:"A1",applied:true},{id:"m2",stage:"Bảng A",court:2,time:"18:00",a:"A2",b:"A3",status:"done",sets:[[11,6],[10,12],[11,7]],winner:"A2",applied:true},{id:"m3",stage:"Bảng A",court:1,time:"18:45",a:"A1",b:"A3",status:"live",sets:[[11,8]],current:[7,5]},{id:"m4",stage:"Bảng B",court:2,time:"18:45",a:"B1",b:"B2",status:"live",sets:[],current:[9,6]},{id:"m5",stage:"Bảng B",court:3,time:"19:20",a:"B2",b:"B3",status:"wait",sets:[],current:[0,0]},{id:"m6",stage:"Bảng B",court:4,time:"19:20",a:"B1",b:"B3",status:"wait",sets:[],current:[0,0]},{id:"m7",stage:"Bán kết 1",court:1,time:"20:15",a:"A1",b:"B2",status:"wait",sets:[],current:[0,0]},{id:"m8",stage:"Bán kết 2",court:2,time:"20:15",a:"B1",b:"A2",status:"wait",sets:[],current:[0,0]},{id:"m9",stage:"Chung kết",court:1,time:"21:00",a:"TBD",b:"TBD",status:"wait",sets:[],current:[0,0]}],audit:[{time:"20:04",user:"BTC Nguyễn Hoàng",action:"Cập nhật lịch thi đấu",detail:"M7 → Sân 1 lúc 20:15"},{time:"19:52",user:"Trọng tài Lan",action:"Chốt kết quả",detail:"M2: A2 thắng A3 2–1"},{time:"19:31",user:"Super Admin",action:"Điều chỉnh seed",detail:"A1 từ seed #2 → #1"}]};

const clone=v=>JSON.parse(JSON.stringify(v));let state=JSON.parse(localStorage.getItem("pickle-tour-state")||"null")||clone(seed);let page="dashboard";let activeMatch=null;let undoStack=[];const $=s=>document.querySelector(s);const teamName=id=>state.teams.find(t=>t.id===id)?.name||(id==="TBD"?"Chưa xác định":id);const save=()=>localStorage.setItem("pickle-tour-state",JSON.stringify(state));const toast=msg=>{const el=document.createElement("div");el.className="toast";el.textContent=msg;document.body.appendChild(el);setTimeout(()=>el.remove(),2200)};const statusBadge=s=>s==="live"?'<span class="badge live">ĐANG ĐẤU</span>':s==="done"?'<span class="badge done">KẾT THÚC</span>':s==="open"?'<span class="badge blue">ĐANG MỞ</span>':'<span class="badge wait">CHỜ</span>';const resultText=m=>!m.sets?.length?"—":m.sets.map(x=>x.join("-")).join(" / ");

function renderNav(){$("#nav").innerHTML=NAV.map(([id,ico,label])=>`<button class="nav-btn ${page===id?"active":""}" data-page="${id}"><span class="ico">${ico}</span>${label}</button>`).join("");document.querySelectorAll("[data-page]").forEach(b=>b.onclick=()=>{page=b.dataset.page;$("#sidebar").classList.remove("open");render()})}
function setHeader(title,sub){$("#pageTitle").textContent=title;$("#pageSub").textContent=sub}
function matchCard(m){return `<div class="match-card"><div class="match-top"><span>${m.time} • Sân ${m.court} • ${m.stage}</span>${statusBadge(m.status)}</div><div class="versus"><div class="team"><strong>${teamName(m.a)}</strong><small>${state.teams.find(t=>t.id===m.a)?.club||""}</small></div><div class="vs">${m.status==="done"?`<span class="score-final">${resultText(m)}</span>`:"VS"}</div><div class="team"><strong>${teamName(m.b)}</strong><small>${state.teams.find(t=>t.id===m.b)?.club||""}</small></div></div><div class="actions" style="margin-top:12px"><button class="mini" data-score-match="${m.id}">✎ Nhập điểm</button><button class="mini">Chi tiết</button></div></div>`}
function dashboard(){const live=state.matches.filter(m=>m.status==="live").length,done=state.matches.filter(m=>m.status==="done").length;setHeader("Tổng quan","Điều hành giải đấu theo thời gian thực");return `<div class="kpis"><div class="kpi"><span class="label">GIẢI ĐANG VẬN HÀNH</span><strong>1</strong><small>+ 2 giải sắp mở</small></div><div class="kpi"><span class="label">TRẬN ĐANG LIVE</span><strong>${live}</strong><small>${done} trận đã hoàn tất</small></div><div class="kpi"><span class="label">VĐV / ĐỘI</span><strong>48</strong><small>32 đã check-in</small></div><div class="kpi"><span class="label">DOANH THU DEMO</span><strong>18,6tr</strong><small>82% đã xác nhận</small></div></div><div class="grid-2"><div class="panel"><div class="panel-head"><div><h2>Live Center</h2><p>Trận đang diễn ra và trận sắp tới</p></div><button class="mini" data-goto="scores">Mở nhập điểm</button></div>${state.matches.filter(m=>m.status!=="done").slice(0,4).map(matchCard).join("")}</div><div class="panel"><div class="panel-head"><div><h2>Hoạt động gần đây</h2><p>Audit log quản trị</p></div></div>${state.audit.slice(0,6).map(a=>`<div style="padding:11px 0;border-bottom:1px solid #edf1ef"><strong style="font-size:13px">${a.action}</strong><div style="color:#73847b;font-size:11px;margin-top:4px">${a.time} • ${a.user}</div><div style="font-size:12px;margin-top:4px">${a.detail}</div></div>`).join("")}</div></div>`}
function tournamentCard(t){return `<article class="tour-card"><div class="tour-cover"><strong>${t.format}</strong>${statusBadge(t.status)}</div><div class="tour-body"><h3>${t.name}</h3><div class="meta"><span>📅 ${t.date}</span><span>📍 ${t.venue}</span><span>👥 ${t.teams} đội đăng ký</span></div><div class="actions"><button class="mini" data-goto="matches">Lịch đấu</button><button class="mini" data-goto="standings">BXH</button><button class="mini" data-goto="bracket">Bracket</button></div></div></article>`}
function tournaments(){setHeader("Giải đấu","Tạo và quản lý toàn bộ giải pickleball");return `<div class="panel-head"><div><h2>Danh sách giải</h2><p>${state.tournaments.length} giải trong hệ thống demo</p></div><button class="btn primary" data-action="newTournament">＋ Tạo giải mới</button></div><div class="page-grid">${state.tournaments.map(tournamentCard).join("")}</div>`}
function players(){setHeader("VĐV & cặp đấu","Quản lý hồ sơ, CLB, trình và đội tham dự");return `<div class="panel"><div class="panel-head"><div><h2>Cặp đấu đang tham gia</h2><p>Demo nhóm A/B</p></div><div class="actions"><button class="mini">Import CSV</button><button class="btn primary" data-action="newTeam">＋ Thêm cặp</button></div></div><div class="table-wrap"><table class="table"><thead><tr><th>Mã</th><th>Cặp VĐV</th><th>CLB</th><th>Bảng</th><th>Thắng</th><th>Thua</th><th>Hiệu số</th></tr></thead><tbody>${state.teams.map(t=>`<tr><td><b>${t.id}</b></td><td>${t.name}</td><td>${t.club}</td><td><span class="badge blue">Bảng ${t.group}</span></td><td>${t.w}</td><td>${t.l}</td><td>${t.pf-t.pa>0?"+":""}${t.pf-t.pa}</td></tr>`).join("")}</tbody></table></div></div>`}
function matches(scoreOnly=false){setHeader(scoreOnly?"Nhập điểm":"Lịch & trận đấu",scoreOnly?"Giao diện nhanh cho BTC và trọng tài":"Điều phối sân, giờ đấu và trạng thái trận");const arr=scoreOnly?state.matches.filter(m=>m.status!=="done"):state.matches;return `<div class="filters"><select><option>Tất cả sân</option><option>Sân 1</option><option>Sân 2</option></select><select><option>Tất cả trạng thái</option><option>Đang đấu</option><option>Chờ</option></select><input placeholder="Tìm VĐV / mã trận..."></div><div class="grid-2"><div class="panel"><div class="panel-head"><div><h2>${scoreOnly?"Các trận cần nhập":"Lịch thi đấu hôm nay"}</h2><p>${arr.length} trận hiển thị</p></div></div>${arr.map(matchCard).join("")||'<div class="empty">Không có trận nào.</div>'}</div><div class="panel"><div class="panel-head"><div><h2>Tình trạng sân</h2><p>Live court monitor</p></div></div>${[1,2,3,4].map(c=>{const m=state.matches.find(x=>x.court===c&&x.status==="live");return `<div style="display:flex;justify-content:space-between;padding:14px 0;border-bottom:1px solid #edf1ef"><div><strong>Sân ${c}</strong><div style="font-size:12px;color:#73847b;margin-top:4px">${m?teamName(m.a)+" vs "+teamName(m.b):"Đang trống"}</div></div>${m?'<span class="badge live">LIVE</span>':'<span class="badge done">TRỐNG</span>'}</div>`}).join("")}</div></div>`}
function calcGroup(g){return state.teams.filter(t=>t.group===g).sort((a,b)=>b.w-a.w||((b.pf-b.pa)-(a.pf-a.pa))||b.pf-a.pf)}
function standings(){setHeader("Bảng xếp hạng","Tự tính thắng/thua, hiệu số và thứ hạng vòng bảng");return ["A","B"].map(g=>`<div class="panel" style="margin-bottom:16px"><div class="panel-head"><div><h2>Bảng ${g}</h2><p>Ưu tiên: số trận thắng → hiệu số → điểm ghi</p></div></div><div class="standing-row head"><span>Hạng</span><span>Đội</span><span>W</span><span>L</span><span>PF</span><span>PA</span><span>+/-</span></div>${calcGroup(g).map((t,i)=>`<div class="standing-row"><span class="rank">${i+1}</span><span><b>${t.name}</b><small style="display:block;color:#73847b">${t.club}</small></span><span>${t.w}</span><span>${t.l}</span><span>${t.pf}</span><span>${t.pa}</span><span><b>${t.pf-t.pa>0?"+":""}${t.pf-t.pa}</b></span></div>`).join("")}</div>`).join("")}
function bracketMatch(m){if(!m)return"";return `<div class="bracket-match"><div class="bracket-team ${m.winner===m.a?"win":""}"><span>${teamName(m.a)}</span><b>${m.sets?.filter(s=>s[0]>s[1]).length||""}</b></div><div class="bracket-team ${m.winner===m.b?"win":""}"><span>${teamName(m.b)}</span><b>${m.sets?.filter(s=>s[1]>s[0]).length||""}</b></div><button class="mini" style="margin-top:7px" data-score-match="${m.id}">Nhập điểm</button></div>`}
function bracket(){setHeader("Bracket","Nhánh loại trực tiếp tự nhận đội thắng");const semi=[state.matches.find(m=>m.id==="m7"),state.matches.find(m=>m.id==="m8")],final=state.matches.find(m=>m.id==="m9");return `<div class="panel"><div class="panel-head"><div><h2>Knock-out</h2><p>Nhấn nhập điểm tại trận để cập nhật người đi tiếp</p></div></div><div class="bracket"><div class="round"><h3>BÁN KẾT</h3>${semi.map(bracketMatch).join("")}</div><div class="round"><h3>CHUNG KẾT</h3><div style="margin-top:55px">${bracketMatch(final)}</div></div><div class="round"><h3>VÔ ĐỊCH</h3><div class="bracket-match" style="margin-top:110px;text-align:center;padding:22px"><div style="font-size:34px">🏆</div><strong>${final?.winner?teamName(final.winner):"Chưa xác định"}</strong></div></div></div></div>`}
function courts(){setHeader("Sân thi đấu","Theo dõi công suất và trận trên từng sân");return `<div class="page-grid">${[1,2,3,4,5,6].map(c=>{const live=state.matches.find(m=>m.court===c&&m.status==="live"),next=state.matches.find(m=>m.court===c&&m.status==="wait");return `<div class="panel"><div class="panel-head"><h2>Sân ${c}</h2>${live?'<span class="badge live">LIVE</span>':'<span class="badge done">TRỐNG</span>'}</div><p style="font-size:13px"><b>Hiện tại:</b> ${live?teamName(live.a)+" vs "+teamName(live.b):"Không có trận"}</p><p style="font-size:12px;color:#73847b"><b>Tiếp theo:</b> ${next?next.time+" • "+teamName(next.a)+" vs "+teamName(next.b):"Chưa xếp"}</p></div>`}).join("")}</div>`}
function simple(title,sub,body){setHeader(title,sub);return `<div class="panel"><div class="panel-head"><div><h2>${title}</h2><p>${sub}</p></div></div>${body}</div>`}
function auditPage(){return simple("Nhật ký hệ thống","Theo dõi mọi thay đổi quan trọng",state.audit.map(a=>`<div style="padding:13px 0;border-bottom:1px solid #edf1ef"><b>${a.action}</b><div style="font-size:12px;color:#73847b;margin-top:4px">${a.time} • ${a.user} • ${a.detail}</div></div>`).join(""))}
function render(){renderNav();const pages={dashboard,tournaments,players,matches:()=>matches(false),scores:()=>matches(true),standings,bracket,courts,referees:()=>simple("Trọng tài","Phân công quyền nhập điểm",'<div class="empty">Module tài khoản và phân trận nằm trong scope sau khi nối backend.</div>'),payments:()=>simple("Thanh toán","Đối soát lệ phí và biên lai",'<div class="empty">Module thanh toán chưa bật ở vertical slice này.</div>'),audit:auditPage,settings:()=>simple("Cấu hình","Rule điểm, thể thức và branding",'<div class="form-grid"><label class="field">Best of<select><option>Best of 3</option><option>Best of 1</option><option>Best of 5</option></select></label><label class="field">Điểm mặc định<select><option>11</option><option>15</option><option>21</option></select></label><label class="field full">Quy tắc xếp hạng<textarea rows="4">Số trận thắng → đối đầu → hiệu số điểm → tổng điểm ghi.</textarea></label></div>')};$("#content").innerHTML=(pages[page]||dashboard)();bindDynamic()}
function bindDynamic(){document.querySelectorAll("[data-goto]").forEach(b=>b.onclick=()=>{page=b.dataset.goto;render()});document.querySelectorAll("[data-score-match]").forEach(b=>b.onclick=()=>openScore(b.dataset.scoreMatch));document.querySelectorAll('[data-action="newTournament"]').forEach(b=>b.onclick=openTournamentModal);document.querySelectorAll('[data-action="newTeam"]').forEach(b=>b.onclick=openTeamModal)}
function openScore(id){activeMatch=state.matches.find(m=>m.id===id);if(!activeMatch)return;if(activeMatch.a==="TBD"||activeMatch.b==="TBD"){toast("Chưa xác định đủ hai đội.");return}activeMatch.current=activeMatch.current||[0,0];undoStack=[];$("#scoreMeta").textContent=`${activeMatch.stage} • Sân ${activeMatch.court} • ${activeMatch.time}`;$("#scoreTitle").textContent=`Trận ${activeMatch.id.toUpperCase()}`;$("#teamAName").textContent=teamName(activeMatch.a);$("#teamBName").textContent=teamName(activeMatch.b);syncScoreDialog();$("#scoreDialog").showModal()}
function syncScoreDialog(){$("#scoreA").textContent=activeMatch.current?.[0]||0;$("#scoreB").textContent=activeMatch.current?.[1]||0;$("#scoreSets").innerHTML=activeMatch.sets?.length?activeMatch.sets.map((s,i)=>`<span class="set-chip">Set ${i+1}: ${s[0]}–${s[1]}</span>`).join(""):'<span style="font-size:12px;color:#73847b">Chưa có set hoàn tất</span>'}
function pushUndo(){undoStack.push(clone(activeMatch));if(undoStack.length>20)undoStack.shift()}
function updateScore(side,delta){pushUndo();const i=side==="a"?0:1;activeMatch.current[i]=Math.max(0,activeMatch.current[i]+delta);activeMatch.status="live";save();syncScoreDialog();render()}
function validSet(){const[a,b]=activeMatch.current,target=Number($("#targetScore").value),by2=$("#winByTwo").checked,hi=Math.max(a,b),lo=Math.min(a,b);return hi>=target&&(!by2||hi-lo>=2)}
function finishSet(){if(!validSet()){toast("Điểm set chưa đủ điều kiện thắng.");return}pushUndo();activeMatch.sets.push([...activeMatch.current]);activeMatch.current=[0,0];save();syncScoreDialog();toast("Đã lưu set.")}
function applyMatchToStandings(m){if(!m.stage.startsWith("Bảng")||m.applied)return;const a=state.teams.find(t=>t.id===m.a),b=state.teams.find(t=>t.id===m.b);if(!a||!b)return;let ap=0,bp=0;m.sets.forEach(s=>{ap+=s[0];bp+=s[1]});a.pf+=ap;a.pa+=bp;b.pf+=bp;b.pa+=ap;if(m.winner===a.id){a.w++;b.l++}else{b.w++;a.l++}m.applied=true}
function finishMatch(){if(activeMatch.current.some(x=>x>0)){if(!validSet()){toast("Set hiện tại chưa đủ điều kiện.");return}activeMatch.sets.push([...activeMatch.current]);activeMatch.current=[0,0]}if(!activeMatch.sets.length){toast("Chưa có kết quả set.");return}const aw=activeMatch.sets.filter(s=>s[0]>s[1]).length,bw=activeMatch.sets.filter(s=>s[1]>s[0]).length;if(aw===bw){toast("Hai đội đang hòa số set.");return}pushUndo();activeMatch.winner=aw>bw?activeMatch.a:activeMatch.b;activeMatch.status="done";applyMatchToStandings(activeMatch);if(activeMatch.id==="m7")state.matches.find(m=>m.id==="m9").a=activeMatch.winner;if(activeMatch.id==="m8")state.matches.find(m=>m.id==="m9").b=activeMatch.winner;state.audit.unshift({time:new Date().toLocaleTimeString("vi-VN",{hour:"2-digit",minute:"2-digit"}),user:"Người dùng hiện tại",action:"Chốt kết quả",detail:`${activeMatch.id.toUpperCase()}: ${teamName(activeMatch.winner)} thắng • ${resultText(activeMatch)}`});save();$("#scoreDialog").close();render();toast("Đã chốt kết quả và cập nhật hệ thống.")}
function undo(){const prev=undoStack.pop();if(!prev){toast("Không còn thao tác để Undo.");return}const idx=state.matches.findIndex(m=>m.id===prev.id);state.matches[idx]=prev;activeMatch=state.matches[idx];save();syncScoreDialog();render();toast("Đã hoàn tác.")}
function openTournamentModal(){$("#genericTitle").textContent="Tạo giải mới";$("#genericBody").innerHTML=`<div class="form-grid"><label class="field full">Tên giải<input id="fName" placeholder="VD: Bình Lợi Open 2026"></label><label class="field">Ngày thi đấu<input id="fDate" type="date"></label><label class="field">Thể thức<select id="fFormat"><option>Men Doubles</option><option>Mixed Doubles</option><option>Women Doubles</option><option>Singles</option></select></label><label class="field full">Địa điểm<input id="fVenue" placeholder="Tên sân / CLB"></label><div class="field full"><button type="button" class="btn primary" id="saveTournament">Tạo giải</button></div></div>`;$("#genericDialog").showModal();$("#saveTournament").onclick=()=>{const name=$("#fName").value.trim();if(!name){toast("Nhập tên giải.");return}state.tournaments.unshift({id:"t"+Date.now(),name,date:$("#fDate").value||"Chưa chốt",venue:$("#fVenue").value||"Chưa chốt",format:$("#fFormat").value,status:"open",teams:0});save();$("#genericDialog").close();render();toast("Đã tạo giải.")}}
function openTeamModal(){$("#genericTitle").textContent="Thêm cặp VĐV";$("#genericBody").innerHTML=`<div class="form-grid"><label class="field full">Tên cặp<input id="teamName" placeholder="VĐV 1 / VĐV 2"></label><label class="field">CLB<input id="teamClub"></label><label class="field">Bảng<select id="teamGroup"><option>A</option><option>B</option></select></label><div class="field full"><button type="button" class="btn primary" id="saveTeam">Thêm cặp</button></div></div>`;$("#genericDialog").showModal();$("#saveTeam").onclick=()=>{const n=$("#teamName").value.trim();if(!n){toast("Nhập tên cặp.");return}const g=$("#teamGroup").value,id=g+(state.teams.filter(t=>t.group===g).length+1);state.teams.push({id,name:n,club:$("#teamClub").value||"Tự do",group:g,w:0,l:0,pf:0,pa:0});save();$("#genericDialog").close();render();toast("Đã thêm cặp.")}}
document.addEventListener("click",e=>{const b=e.target.closest("[data-score]");if(b&&activeMatch)updateScore(b.dataset.score,Number(b.dataset.delta))});$("#finishSet").onclick=finishSet;$("#finishMatch").onclick=finishMatch;$("#undoScore").onclick=undo;$("#mobileMenu").onclick=()=>$("#sidebar").classList.toggle("open");$("#quickTournament").onclick=openTournamentModal;$("#roleSelect").onchange=e=>toast("Đã chuyển vai trò: "+e.target.options[e.target.selectedIndex].text);$("#resetDemo").onclick=()=>{state=clone(seed);save();render();toast("Đã khôi phục dữ liệu demo.")};render();